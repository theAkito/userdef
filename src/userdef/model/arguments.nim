const invalidId* = -1

type
  Arguments        * = object
    configPath     * : string
    long           * : bool
    home           * : string
    name           * : string
    uid            * : int
    gid            * : int